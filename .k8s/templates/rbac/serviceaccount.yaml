{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "slim4-app.serviceAccountName" . }}
  labels:
    {{- include "slim4-app.labels" . | nindent 4 }}
{{- end }}
